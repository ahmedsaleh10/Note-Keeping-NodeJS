# Note-Keeping-NodeJS
The aim of this repo to create an api for task tracker project to handle CRUD operations.
## Usage
- At first clone this repo on your device.
- Then run a nodemon command (i use this instead of node to stop and start the server automatically after each change)
